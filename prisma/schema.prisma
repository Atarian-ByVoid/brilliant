generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ASSCII {
  id       String   @id @default(uuid())
  criadoEm DateTime @default(now())

  field1 String
  field2 Decimal

  @@map("tb_ascii")
}
